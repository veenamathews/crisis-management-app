{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "nodemon built/index.js",
    "build": "esbuild app.js --platform=node --bundle --minify --outfile=built/index.js --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@mtproto/core": "6.2.0",
    "express": "^4.17.3",
    "prettier": "^2.6.2"
  },
  "devDependencies": {
    "esbuild": "^0.14.32"
  }
}
